# Invoice Processing Assistant

The finance team wants a tool that accepts supplier invoices and extracts key fields such as vendor name, invoice number, due date, line items, total amount, currency, VAT, and payment terms. The goal is to reduce manual copying from PDFs into the accounting system.

The first version should support drag-and-drop upload and show extracted data for review before export.
